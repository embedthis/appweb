#
#   appweb.conf -- Monitor and Defense test configuration
#

Include ../appweb.conf

Defense deny REMEDY=ban STATUS=406 MESSAGE="Client temporarily banned due to monitored limit exceeded" PERIOD=5secs
Monitor "NotFoundErrors > 190" 5sec deny
